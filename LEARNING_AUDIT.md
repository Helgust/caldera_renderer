# Caldera — Comprehension Audit

A module-by-module "rewind" of the renderer. Much of this code was adapted from a
Vulkan tutorial base and split up with AI help under time pressure, so the goal here
is to **back-fill ownership**: understand code that already works.

## How to use this

For each module, before reading it closely, try to answer from memory:

1. **Responsibility** — what is this module's job, in one sentence?
2. **Why separate** — what would break / get worse if I inlined it?
3. **Core calls** — the 2–3 Vulkan calls at its heart, and *why those*?
4. **Decisions** — what did I (or the AI) choose here that could've gone differently?

If I can't answer → that's the module to study next. Work with Claude as a Socratic
checker: explain a module in my own words, Claude probes the thin spots and points at
exact lines. **I do the thinking; Claude finds the holes.** No hand-fed answers.

Status legend: 🔴 not started · 🟡 in progress · 🟢 understood & can explain unaided

## Modules

| Status | Module | Notes / open questions |
|---|---|---|
| 🟢 | [core/vulkanContext](source/core/vulkanContext.cpp) | ✅ spine: `vkCreateInstance`→`vkCreateDevice`→`vmaCreateAllocator`. ✅ **features vs extensions** are different axes (features = capability toggles via `vk12`/`vk13`+`pEnabledFeatures`; extensions = API surface by name string, e.g. swapchain). ✅ features are negotiated **only at `vkCreateDevice`** — instance knows nothing about device caps; `vkEnumeratePhysicalDevices` does **not** filter (it's `ls`, not `find`); `assert` only guards the array index. ✅ **no device suitability check** → blindly takes `devices[0]`; if it lacks a feature, `vkCreateDevice`→`VK_ERROR_FEATURE_NOT_PRESENT`→`vkCheck` prints to stderr + `exit()` = hard crash with extra steps (app vanishes, no window). Safe *only* because dev box has one capable discrete GPU. ✅ surface/swapchain live **outside** by responsibility **and** lifetime (device is created once & immutable; swapchain rebuilds on resize; surface needs a third-party-windowed OS handle). ✅ two-call enumerate idiom (count, then fill). Minor: `vkCheck` treats `VK_INCOMPLETE` as fatal (only tests `!=VK_SUCCESS`) — latent, can't trigger today (count-first idiom); **ticketed: Notion 1.10**. |
| 🟢 | [core/swapchain](source/core/swapchain.cpp) | ✅ **format hardcoded** `B8G8R8A8_SRGB`+`SRGB_NONLINEAR` with no `vkGetPhysicalDeviceSurfaceFormatsKHR` query → same blind-`devices[0]` pattern; unsupported pair = garbage/validation error, "safe" only because dev surface happens to expose it. ✅ **image count** passes `caps.minImageCount` straight → `+1` is the best-practice fix, and the *real why* is present-engine ownership: with exactly the min, the engine holds one for scanout and `vkAcquireNextImageKHR` can find none free → CPU stalls waiting on the next image (frame-pacing hitch), not "1 is too few". ✅ **extent** built from window `size`, ignores `caps.currentExtent`: the `0xFFFFFFFF` sentinel = "swapchain, you choose"; a *concrete* value = compositor dictates the size (e.g. Wayland) and ignoring it is wrong/validation error. Also unclamped to min/maxImageExtent. Surface-caps fix **ticketed: Notion 1.8**. ✅ **present mode FIFO** = always-available safe choice (no mailbox). ✅ **recreate ordering**: destroys old swapchain immediately after creating new (L55); spec forbids destroy while acquired images have outstanding ops → legal **only** because the *caller* (main.cpp:523) does `vkDeviceWaitIdle` first. Precondition is caller-enforced & undocumented = fragility; that one idle is **amortized** across swapchain + depth recreate, so burying it in `recreate` isn't a clean win. Fix **ticketed: Notion 1.7**. |
| 🟢 | [core/syncObjects](source/core/syncObjects.h) | ✅ semaphores = GPU↔GPU ordering, fences = GPU→CPU resource reuse; ✅ derived cold that `fence[frameIndex]` covers frame **N−2** not N−1 → an in-flight frame's writes to a *shared* resource aren't protected. Fix for the shared depth image is **ticketed: Notion 1.6** (per-frame depth ring). |
| 🟢 | [core/commandManager](source/core/commandManager.h) | ✅ **pool flag** `RESET_COMMAND_BUFFER_BIT` = a *choice*: per-buffer reset (`vkResetCommandBuffer`) vs whole-pool reset (`vkResetCommandPool`). State machine: reset→**initial**, `vkBeginCommandBuffer`→**recording** (with flag set, Begin does an *implicit* reset first: executable→initial→recording). ✅ derived that the explicit `vkResetCommandBuffer` at main.cpp:390 is **redundant** — Begin would reset anyway; harmless belt-and-suspenders, not a bug. ✅ **single shared pool forces per-buffer reset**: `vkResetCommandPool` would reset *all* FramesInFlight buffers at once, but the fence at main.cpp:340 only proves the *current* slot is done — the other slot may still be **pending** → pool-reset unsafe. The scaling alt (Design B) = **one pool per frame-in-flight**, each gated by its own fence; current single-pool + per-buffer reset is the *simplest correct* option while recording is single-threaded. ✅ **what protects the reset is the fence, not the flag** — same N−2 guarantee from syncObjects, applied to the CB instead of the depth image. ✅ **`submitOneTime`**: fence vs `vkQueueWaitIdle` = *blast radius* (this-submit vs queue-wide drain), both block CPU; mid-loop call = full CPU→GPU→CPU round-trip → **pipeline bubble / frame spike** → hence init-only. Creates+destroys a `VkFence` per call (fine at init). ✅ **pool sharing safe** by the *external-synchronization* rule (a pool may not be touched from 2 threads at once) — safe only because `submitOneTime` runs pre-loop, single-threaded; would break if called from a worker thread mid-`vkBeginCommandBuffer` on the same pool. |
| 🟢 | [core/debugUtils](source/core/debugUtils.h) | ✅ **read-only wrappers** — module doesn't *enable* anything; it reads `gDebugUtilsEnabled`, set once at [vulkanContext.cpp:91](source/core/vulkanContext.cpp#L91). ✅ **coupling caught**: flag chained to the *validation layer* (`= enableValidation`), but naming/labels come from the *`VK_EXT_debug_utils` extension* — so naming is only ever on when validation is. Limitation, not a law: RenderDoc implements debug-utils, so naming a *release* build is legitimate but currently impossible (convenience coupling, not fundamental). ✅ **`uint64_t` handle type-erasure** matches the Vulkan struct field; cost = caller passes handle *and* `type` separately with nothing tying them → wrong `type` is **not** a compile error, surfaces at runtime as a validation error / mislabeled object. ✅ **`inline` variable (C++17, L13)**: one shared instance across all TUs. Drop `inline` → multiple-definition **linker error** (loud); use `static` instead → **per-TU copies, silent bug** (vulkanContext sets its copy, framegraph reads its own still-`false` copy). `inline` is what makes the cross-TU write visible. ✅ **not zero-cost in release**: `gDebugUtilsEnabled` is a *runtime mutable global* — from framegraph.cpp's TU the compiler can't prove its value (another TU may write it), so it's **forced** to emit load+test+branch on every call even in release. True zero-cost needs a *compile-time* constant (`constexpr` + `#ifdef NDEBUG` + `if constexpr`) → body discarded entirely; tradeoff = lose runtime toggle (release+RenderDoc naming would need a recompile). **Strip-in-release fix ticketed: Notion 1.11.** ✅ **decisions**: `labelColor` FNV-1a hash → stable distinct hue per pass name (vs fixed palette / manual color); `DebugLabelScope` RAII → can't-forget + exception-safe begin/end, cost = region bound to a C++ scope (can't open in one fn, close in another). |
| 🟢 | [core/gpuTimer](source/core/gpuTimer.h) | ✅ **job**: double-buffered GPU timestamp profiler — brackets a pass with two `vkCmdWriteTimestamp2`, diffs the ticks, ×`timestampPeriod`→ms. ✅ **query state machine**: a slot is *unavailable* after `vkCmdResetQueryPool`, *available* once the GPU writes it; **must reset before every write** (writing an un-reset query = UB). Reset is a `vkCmd...` (GPU timeline), not a host call, so it's ordered *relative to the writes* in the same command stream. ✅ **lazy readback never blocks** by the **N−2 fence rule** (same as syncObjects/commandManager): by the time `beginFrame(frameIndex)` reads slot N, the caller already waited that slot's fence → data retired → `vkGetQueryPoolResults` (no `WAIT` bit) finds it available. 🐛 **FOUND + FIXED a real defect (not a tradeoff, not dev-box-only):** `init`/`beginFrame` self-assigned params over same-named **members** (L26-27, L55) → members `maxPassesPerFrame`/`frameIndex` stuck at **0**. First-order effect: `beginPass` guard `passCount >= maxPassesPerFrame` = `0 >= 0` → **always early-returns → no timestamp ever written**, double-buffering moot (the slot-collision/UB-on-write were downstream of a gate that never opens). Fixed at root by renaming the *params* (`frames_in_flight`/`max_passes_per_frame`/`frame_index`) so the assignments became genuine member writes. ✅ key lesson — **"works" ≠ "correct"**: it shipped producing plausible-looking output only because (a) validation was off ([vulkanContext.cpp:49](source/core/vulkanContext.cpp#L49) `wantValidation=false`) and (b) the dev driver tolerated it — *unfalsified*, same family as blind `devices[0]` / hardcoded swapchain format. Proven by flipping `wantValidation=true`: validation **storm pre-fix → clean post-fix**. ✅ **intentional** shadow at L59 (local `passCount` = retired frame's count) is correct & distinct from the accidental kind — shadowing isn't evil, *unintentional* shadowing is. Robustness hardening (return-value/`NOT_READY` handling, `timestampValidBits` masking, queue-support check, `-Wshadow` in build) **ticketed: Notion 1.13.** |
| 🟢 | [rendering/framegraph](source/rendering/framegraph.cpp) | ✅ barriers = exec + memory + layout; ✅ state tracking via `r.state`; ✅ *why* state doesn't persist across frames (graph rebuilt each frame, `importImage` resets to `{}`). ✅ **`createImage` already exists** (L81) — allocates a transient via `Image::create2D`, `owned=true` so `reset()` destroys it — but is **never called**; main.cpp imports everything. So the "allocate" half is in code, just unexercised; "alias" half genuinely absent (header L68). ✅ **per-frame churn caught**: `FrameGraph fg(ctx)` is stack-scoped *inside* the render loop ([main.cpp:403](source/main.cpp#L403)), so ctor+dtor (→`reset()`→destroy owned, L201) fire **every frame**. A `createImage` in the loop = full `vmaCreateImage`+`vkCreateImageView` / destroy **per frame, forever** — *that* is the cost a real transient pool dodges by allocating backing memory **once at compile** and just handing out views. createImage is safe only at setup, not per-frame → which is why the loop quietly avoids it. ✅ **barrier engine is general, not attachment-only**: state struct carries `(layout,stage,access)`, so producer→consumer through `SampledRead` (ColorAttachment-write → fragment-shader/sampled-read) produces a **correct** barrier today, untested but sound. What's attachment-only is the *attachment setup* in `beginRendering` (L104-163) — a SampledRead resource gets a correct barrier but no rendering attachment (right — you don't render *into* a sampled image). Don't conflate the barrier engine with the attachment engine. ✅ **aliasing = per-memory hazard, not per-resource** (the green-maker): aliasing needs (a) lifetime intervals (first-write→last-read) to prove two transients never co-exist, AND (b) an **aliasing barrier** when memory is reused — `src = {dead resource X's last write stage/access}`, `dst = {new resource Y's first write}`. Without it, X's writes can still be draining when Y starts writing the same memory → **WAW hazard at the memory level** (declaration order ≠ GPU execution order — same lesson as N−2 fence). The loop at L172-180 **structurally cannot** emit it: it barriers each resource against *its own* `r.state`, and Y's state is UNDEFINED — blind to X having squatted in that memory. → aliasing needs per-memory-region tracking, not per-resource = why it's not a quick patch. Transient pool + aliasing remain a Phase-3 milestone (Known gaps). |
| 🟢 | [rendering/pipeline](source/rendering/pipeline.cpp) | ✅ **what a `VkPipeline` *is*:** a pre-baked blob of hardware config (compiled shaders + all fixed-function state). The **driver** (CPU) translates high-level state → registers/machine code; Vulkan forces that translation to happen **at `vkCreateGraphicsPipelines`** instead of (GL-style) at draw time → no mid-loop "driver hitch", cost paid up front & predictably. ✅ **immutable** because the bake depends on the exact state (depth-on vs off can yield different compiled code/register layout); to vary → build a **second pipeline**, don't mutate. At draw time the driver just **binds** (`vkCmdBindPipeline` ≈ pointer/register swap) — that's why Vulkan draws are cheap. ✅ **builder = separate the CreateInfo scaffolding**; fluent setters + sane defaults so passes differ only by what they set. ✅ **dynamic state** (viewport+scissor, L71): exempts them from the bake → resize changes extent (ties to swapchain audit) without a **rebuild spike**; set per-draw via `vkCmdSet…`. Cost of dynamic = lose **specialization** (driver compiles a *general* config) + reintroduce per-draw work = the very loop cost pipelines exist to remove → so **bake what's stable, make dynamic only what varies often**. ✅ **dynamic rendering** (VK 1.3, `pNext=&renderingCI`, L103-111): replaced `VkRenderPass`+`VkFramebuffer`+subpasses; pipeline binds to **format *values*** not a `VkRenderPass` *object*. Formats baked for the same specialization reason (fragment output stage compiled around exact pixel layout incl. sRGB-encode-on-write; depth-vs-depth+stencil is a sub-case). Reuse win: one pipeline works with **any** `beginRendering` of **matching formats** — different image *objects/frames*, **not** different formats (those are frozen). ✅ **CreateInfo lifetimes:** all `*CreateInfo` are **transient** — Vulkan consumes every `pNext`/`pStages`/`pAttachments` pointer **synchronously during the create call**, retains none after. So locals dying post-`build()` is safe; `colorFormat_` being a *member* vs `renderingCI` a *local* is **incidental** (both alive during the call) — member-ness would only matter if the create were **deferred**. Real rule = "alive at the synchronous read", not "member=safe/local=dangerous". ⚠️ **baked-in limits (not bugs today):** `colorAttachmentCount=1` hardcoded → **cannot express a depth-only pass** (shadow map writes *no* color) — blocks the framegraph producer→consumer milestone; `blendAttachment` frozen opaque (`colorWriteMask=0xF`, blend disabled) → no transparency/alpha. Also `build()` does **no validation** that required fields were set (forget `setShaderStages`/layout → cryptic validation error deep in create, same family as blind `devices[0]`). |
| 🟢 | [rendering/shader](source/rendering/shader.cpp) | ✅ **three-stage journey, not one**: source text → **Slang IR** (`loadModuleFromSource`, L33, parsed+type-checked, *target-independent*) → **SPIR-V blob** (`getTargetCode(0)`, L36) → **`VkShaderModule`** (`vkCreateShaderModule`, L43, driver ingests). Caught that `getTargetCode` is a *separate* call because IR is the **front-end/back-end split point**: front-end parses **once**, back-end emits **per target** — the `0` indexes the `targets` array set in `init` (L10-11, `SLANG_SPIRV`/`spirv_1_4`); adding `targets[1]` would let the *same IR* emit DXIL/HLSL via `getTargetCode(1)`. ✅ **two-object Slang hierarchy** (header L11-12): **1 global session** = the compiler itself *booted* (loads core module/stdlib — expensive → one per app lifetime, L9 comment), **1+ sessions** = compilation *context/knobs* (targets, `defaultMatrixLayoutMode`, `EmitSpirvDirectly`, search paths, module cache), **N modules** = the per-shader unit. Corrected my own off-by-one: the **module** is per-shader, **not** the session — `loadSlang` *takes* a `session*` (L27), doesn't create one, so all shaders share it. ✅ **ownership asymmetry** (`Slang::ComPtr` RAII refcount vs raw `VkShaderModule` + manual `destroy`) is just an **idiom mismatch** (Slang ships a smart pointer, Vulkan has no RAII), *not* a lifetime argument — and the lifetime is actually **inverted**: a `VkShaderModule` is **transient** (once `vkCreateGraphicsPipelines` bakes the SPIR-V in, it can be destroyed immediately — same "alive only at the synchronous read" rule as the pipeline `*CreateInfo` audit). 🐛 **FOUND a real defect (gpuTimer/`devices[0]` family — unfalsified because untested):** `loadSlang` passes `nullptr` for `loadModuleFromSource`'s **diagnostics blob** out-param (L32-33) **and** never null-checks the returned module → a single `.slang` typo returns null, its *reason is silently dropped*, then L36 `slangModule->getTargetCode` **derefs null → access-violation crash** with zero indication it was a shader error. Also L36 ignores `getTargetCode`'s `SlangResult` + the `spirv` blob is unchecked before L43. **Init/load-time, single-threaded → developer-ergonomics defect, not runtime-stability.** Prerequisite for 4.3 hot-reload (can't "log the error" if it's thrown away). **Ticketed: Notion 1.14.** |
| 🔴 | [resources/buffer](source/resources/buffer.cpp) | VMA usage, host-visible vs device-local, BDA |
| 🔴 | [resources/image](source/resources/image.cpp) | create2D, views, `transitionLayout` barrier construction |
| 🟢 | [scene/sceneLoader](source/scene/sceneLoader.cpp) | ✅ **job**: turn a parsed `tinygltf::Model` into the renderer's own `Vertex`/index/`ImageData` arrays — the *heart isn't* `LoadASCIIFromFile` (that hands back a fully-parsed model), it's the 100 lines after that flatten glTF's indirection into flat buffers. ✅ **glTF's three-level indirection** `accessor → bufferView → buffer` (L33-44): **buffer** = raw bytes; **bufferView** = a byte *slice* (offset+len) **and owns `byteStride`**; **accessor** = how to *interpret* the slice (type `VEC3`, componentType `float`, `count`). Split exists for **sharing**: one buffer → many views → many accessors. That's *why* `ByteStride(view)` needs **both** — POSITION/NORMAL/UV accessors can point at the **same** view and must hop by the same stride (interleaved); stride lives on the *view* so they stay in lockstep. `stride==0` (L90-91 fallback) = glTF saying **tightly packed** → derive stride from accessor element size. ✅ **Q3 corrected**: the interesting work is the extraction, not the load call. 🐛 **hash/equality contract violation [Notion 1.15]:** `unordered_map<Vertex,uint16_t>` (L29) needs `a==b ⇒ hash(a)==hash(b)`, but `operator==` is **epsilon-fuzzy** ([sceneLoader.h:19-24](source/scene/sceneLoader.h#L19-L24)) while `hash` is **bit-exact** ([sceneLoader.h:33-37](source/scene/sceneLoader.h#L33-L37)). Near-equal verts → **different buckets** → `==` *never called* → duplicate inserted. So it's **neither** exact nor fuzzy dedup — advertises fuzzy, silently delivers broken-exact; the epsilon `==` is **dead code**. Unfalsified (`gpuTimer`/`devices[0]` family): indexed glTF reuses the *same bytes* for a shared vertex → `==` & `hash` agree by accident on the only path that runs. Fix leans **A** (drop epsilon, exact) over **B** (true fuzzy needs spatial search — naïve grid-snap fails at cell boundaries). 🐛 **uint16 index truncation [Notion 1.16]:** L80-81 `static_cast<uint16_t>` a `UNSIGNED_INT` index; vertex 66000 → `&0xFFFF` = 464 → triangle refs an unrelated vert = garbage, **no crash/no validation error** (in-bounds, just wrong). Not a 1-char fix: storage type couples to `VK_INDEX_TYPE_UINT16` at bind ([main.cpp:432-433](source/main.cpp#L432-L433)) + size calc ([main.cpp:182](source/main.cpp#L182)). ⚠️ **"single-mesh limit" is mis-named:** the loop (L31-32) iterates **all** meshes×primitives, appending into **one** `scene.mesh` (L127-130) with **one** shared `uniqueVertices` → it *flattens everything into one soup*, not "loads one." Permanently loses per-primitive boundaries (can't draw/transform a sub-mesh alone) & material association (`primitive.material` never read → `scene.images` have no triangle linkage); cross-mesh dedup + the merge also pulls the uint16 overflow threshold *closer* (sum of all verts, not largest mesh). Also: `.gltf` ASCII only (`LoadASCIIFromFile`, L19), no `.glb`; `vertex.color` hardcoded `{1,1,1}` (L123, COLOR_0/material ignored). |
| 🔴 | [ui/imguiLayer](source/ui/imguiLayer.cpp) | ImGui Vulkan backend init, its descriptor pool, draw integration |
| 🔴 | [main.cpp](source/main.cpp) | orchestration: descriptor set (bindless textures), BDA push, render loop, input |

## Known architectural gaps (future milestones, not bugs today)

- **Frame graph: transient pool + aliasing.** `createImage` exists but is unused — and
  unsafe to call in the per-frame loop (graph is stack-scoped → ctor/dtor each frame →
  per-frame `vmaCreateImage`/destroy churn). Real fix: allocate transient backing memory
  **once at compile**, hand out views; add lifetime analysis + **aliasing barriers**
  (per-memory WAW, not per-resource) to share memory between non-overlapping transients.
  Persistent/cross-frame resources (state resets to `{}` each frame today) also blocked.
  Unblocks: producer→consumer passes (shadow map, G-buffer) and temporal techniques (TAA,
  reprojection) that need last frame's contents to survive.
- **Full bindless.** Textures use descriptor indexing, but per-draw still binds a
  descriptor set + pushes BDA. Consolidate toward one set / addresses.
- **Multiple meshes / materials / draw batching.** `scene.mesh` is singular today.
- **GPU-driven rendering.** Plain `vkCmdDrawIndexed` → indirect + culling later.
- **Single depth image vs frames-in-flight** — ✅ understood (real WAW/WAR hazard; fence covers N−2, not N−1). Fix **ticketed: Notion 1.6** (per-frame depth ring).
- **Swapchain hardcodes format + ignores surface caps.** No `vkGetPhysicalDeviceSurfaceFormatsKHR` query (format/colorspace assumed), `caps.currentExtent` ignored (breaks on size-dictating compositors like Wayland), extent unclamped to min/max, `minImageCount` used without `+1`. Portability + frame-pacing milestone, same family as the device-suitability gap. **Ticketed: Notion 1.8** (surface caps) + **1.7** (recreate device-idle precondition).
- **No physical-device suitability check.** `vulkanContext` takes `devices[0]` and requests a fat feature set with zero validation; a non-capable device 0 → `vkCreateDevice` fails → `exit()` deep in init (no window, cryptic stderr). Safe only on a single-capable-GPU dev box. Real fix: enumerate → score with `vkGetPhysicalDeviceFeatures2`/`Properties2`, pick the qualifying device, fail gracefully if none. Portability milestone, not a bug today. **Ticketed: Notion 1.9.**

## Reference

- Book for the *architecture* phase: *Mastering Graphics Programming with Vulkan*
  (assumes fundamentals — which Caldera already has). Read for patterns, implement in
  Caldera's own abstractions; steal architecture, not code.
